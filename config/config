RUN_SCRIPT="run"

CONFIG_DIR="conf"
VENDOR_DIR="vendor"


CACHE_SRC="src"
CACHE_PKG="pkg"

DEFAULT_DIR="default"
CUSTOM_DIR="config"

ROOT_FILES="Procfile"

INDEX_FILES="index.html index.htm index.xhtml index.php"
WWW_DIR="out"


PCRE_VERSION="8.31"
PCRE_TAR="pcre-$PCRE_VERSION.tar.gz"
PCRE_SRC="pcre-$PCRE_VERSION"
PCRE_URL="ftp://ftp.csx.cam.ac.uk/pub/software/programming/pcre/$PCRE_TAR"

NGINX_VERSION="1.2.3"
NGINX_CONFIGURE="--with-pcre=../pcre-$PCRE_VERSION --with-http_stub_status_module --with-http_gzip_static_module --with-http_realip_module"
NGINX_TAR="nginx-$NGINX_VERSION.tar.gz"
NGINX_SRC="nginx-$NGINX_VERSION"
NGINX_URL="http://nginx.org/download/$NGINX_TAR"
NGINX_DIR="nginx"

# PHP_VERSION="5.4.6"
# PHP_CONFIGURE="--enable-fpm --enable-pcntl --enable-inline-optimization --with-mhash --with-iconv --with-curl=/usr --with-mysql --with-pdo-mysql --enable-mbstring --enable-mbregex --with-pcre-regex --with-openssl --enable-zip --with-zlib --with-gd"
# PHP_TAR="php-$PHP_VERSION.tar.gz"
# PHP_SRC="php-$PHP_VERSION"
# PHP_URL="http://us.php.net/distributions/$PHP_TAR"
# PHP_DIR="php"